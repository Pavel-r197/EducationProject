name: my_education_project
services:
  postgres-db:
    image: postgres:16
    container_name: postgres16
    environment:
      - POSTGRES_DB=education
      - POSTGRES_USER=user123
      - POSTGRES_PASSWORD=edpass321
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data